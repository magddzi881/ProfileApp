datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Furniture {
  id       Int     @id @default(autoincrement())
  name     String
  category String
  price    Int
  inStock  Boolean
}
